# EntropyLab

> Self-contained, air-gapped Bitcoin key and wallet calculator. Turn dice rolls, seed phrases, and private keys into wallet recovery information — entirely offline.

EntropyLab is a single HTML file with no runtime dependencies. It is designed to be downloaded, moved to a computer that never goes online, and opened in a modern browser. The hosted copy at entropylab.online is for convenience only: do not enter seed phrases, private keys, or other secret wallet material into an internet-connected device.

## What it does

- Accepts dice rolls, coin flips, playing cards, hexadecimal entropy, BIP39 seed phrases (12–24 words), Electrum 2.0+ native seeds (HMAC "Seed version", PBKDF2 salt "electrum", Standard m/0 m/1, SegWit m/0h/0 m/0h/1), extended keys, WIF keys, raw private keys, and Casascius mini keys.
- Derives BIP39 seeds, BIP32 extended keys, master fingerprints, addresses, and Bitcoin Core-compatible descriptors for legacy, nested SegWit, native SegWit, and Taproot; mainnet and testnet. Electrum-native seeds are a separate path from BIP39.
- Builds multisignature wallets, including watch-only multisig from extended public keys alone.
- Inspects PSBT v0, PSBT v2 (BIP370), and raw signed transactions: amounts, fees (PSBT claims), repeated ECDSA nonces, optional anti-exfil transcript checks, inscription envelopes in taproot witnesses (detector only — does not number sats or create inscriptions), session-key output ownership (change vs not yours), and OP_RETURN data-carrier outputs (detector only — does not create them).
- Derives BIP-85 child entropy from a BIP32 root: English BIP-39, WIF, XPRV, HEX, and Base64/Base85 passwords.
- Derives BIP-352 Silent Payment addresses, labeled codes, and BIP-392 descriptors; computes sender taproot outputs and verifies pasted outputs offline. Does not scan the chain.
- Exports a Bitcoin Core wallet.dat (watch-only by default) with all derived descriptors imported.
- Runs startup sanity checks on the host browser and refuses to render if they fail.

## Links

- Site: https://entropylab.online/
- Source: https://github.com/w-s-bitcoin/entropylab
- Releases: https://github.com/w-s-bitcoin/entropylab/releases
